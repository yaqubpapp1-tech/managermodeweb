name = "managercareerweb"
compatibility_date = "2026-08-23"

# Static site: deploy the repository root as Workers Assets.
# This is intentionally configured for `wrangler deploy`, not Pages.
[assets]
directory = "."

[observability]
enabled = true
